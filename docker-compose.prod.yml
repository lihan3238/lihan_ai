x-logging: &default-logging
  driver: json-file
  options:
    max-size: "20m"
    max-file: "5"

services:
  postgres:
    logging: *default-logging

  redis:
    logging: *default-logging

  new-api:
    logging: *default-logging
