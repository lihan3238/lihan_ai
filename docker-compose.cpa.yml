services:
  cli-proxy-api:
    image: ${CLI_PROXY_IMAGE:-eceasy/cli-proxy-api:v7.2.67@sha256:00279b1ef92aa1edf2acb4d414e50502d8ba3a25f91cee0ec86615f272d4f505}
    container_name: relay-cpa
    restart: unless-stopped
    environment:
      DEPLOY: ${CPA_DEPLOY:-}
      TZ: ${TZ:-Asia/Shanghai}
    volumes:
      - ${CPA_CONFIG_PATH:-./data/cpa/config.yaml}:/CLIProxyAPI/config.yaml:ro
      - ${CPA_AUTH_PATH:-./data/cpa}:/root/.cli-proxy-api
      - ${CPA_LOG_PATH:-./logs/cpa}:/CLIProxyAPI/logs
    ports:
      - "${CPA_BIND_IP:-10.22.0.40}:${CPA_UI_PORT:-8317}:8317"
    networks:
      - relay-internal
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"

networks:
  relay-internal:
    name: ${LIHAN_AI_NETWORK:-lihan_ai_relay-internal}
